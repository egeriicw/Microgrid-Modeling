[project]
name = "microgrid-api"
version = "0.1.0"
description = "SNLG platform service layer: FastAPI + RQ worker (depends on snlg)."
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.2",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "redis>=5.0",
    "rq>=1.16",
    "boto3>=1.42",
    "python-multipart>=0.0.9",
    "sse-starlette>=2.1",
    "pyyaml>=6.0",
    # engine (path dependency; see [tool.uv.sources] or install -e ../snlg)
    "snlg",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "httpx>=0.27",
    "fakeredis>=2.23",
    "moto[s3]>=5.0",
    "freezegun>=1.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: exercises the real snlg engine on fixture data",
]

# When the engine lives at ../snlg with its own pyproject:
# [tool.uv.sources]
# snlg = { path = "../snlg", editable = true }
